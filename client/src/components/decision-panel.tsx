import { Check, X } from "lucide-react";
import type { FastingDecisionPreview } from "@/lib/agent-workspace";

export function DecisionPanel({ preview, onClear }: { preview: FastingDecisionPreview | null; onClear: () => void }) {
  if (!preview) return <section className="surface p-5 sm:p-6"><h2 className="font-display text-2xl">Need a decision?</h2><p className="mt-2 text-sm text-muted-foreground">Ask the agent what your options are. It will preview the change before anything happens.</p></section>;
  return <section className="surface p-5 sm:p-6" aria-labelledby="decision-title"><div className="flex items-start justify-between gap-3"><div><h2 id="decision-title" className="font-display text-2xl">{preview.title}</h2><p className="mt-1 text-sm text-muted-foreground">{preview.summary}</p></div><button type="button" onClick={onClear} aria-label="Close decision preview" className="rounded-full p-2 text-muted-foreground hover:bg-white/10 hover:text-foreground"><X className="h-4 w-4" /></button></div><div className="mt-5 grid gap-3">{preview.options.map((option) => <div key={option.id} className="rounded-2xl border border-white/[0.08] bg-white/[0.025] p-4"><div className="flex items-start justify-between gap-3"><div><p className="font-semibold">{option.label}</p><p className="mt-1 text-sm text-muted-foreground">Ends {new Date(option.targetEndTime).toLocaleTimeString([], { hour: "numeric", minute: "2-digit" })}</p><p className="mt-2 text-xs text-muted-foreground">{option.context}</p></div><Check className="h-4 w-4 text-primary" /></div></div>)}</div><p className="mt-4 text-xs text-muted-foreground">Preview only. The app will ask before applying a change.</p></section>;
}
